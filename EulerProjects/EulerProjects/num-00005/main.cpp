#include "num-00005.hpp"


int main(){

	finder number;

	//std::cout << (number.isDivisible(2520, 10) ? "ture" : "false") << std::endl;
	std::cout << number.smallestDividendFind(10)<< std::endl;

	return 0;
}
